providers = ["python", "node"]

[phases.build]
cmds = ["python -m pip install -r requirements.txt", "npm install --prefix website --no-audit --no-fund", "npm run build --prefix website"]

[start]
cmd = "python bot.py"
